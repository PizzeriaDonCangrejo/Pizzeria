
package asdf;

import javax.swing.JOptionPane;

public class pedido {
    
   static ordenes orden=new ordenes();

    public static void llevarO(){
    orden.setLlevar(menu2.getped()); 
    }
    public static void cantidades(){
    orden.setcP(pedir.getcP());
    orden.setCjyq(pedir.getCjyq());
    orden.setCh(pedir.getCh());
    orden.setCs(pedir.getCs());
    orden.setCv(pedir.getCv());
    orden.setCn(pedir.getCn());
    }
    public static void extra(){
    orden.setExtras(extras.getEx());

    }
    
    
    
    
    
    
}
